<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration.dtd">


<hibernate-configuration>
    <session-factory>
        <!-- Configuración de conexión a la BD -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/incidencias?serverTimezone=UTC</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">1234</property>

        <!-- Dialecto actualizado para MySQL 8 -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Mostrar consultas SQL en la consola -->
        <property name="hibernate.show_sql">false</property>
        <property name="hibernate.format_sql">false</property>
        <property name="hibernate.use_sql_comments">false</property>

        <!-- Control de esquema de la BD -->
        <property name="hibernate.hbm2ddl.auto">validate</property>

        <!-- Configuración para manejo de fechas -->
        <property name="hibernate.jdbc.time_zone">UTC</property>

        <!-- Mapeo de clases de entidades -->
        <mapping class="Modelos.Empleados"/>
        <mapping class="Modelos.Incidencias"/>

    </session-factory>
</hibernate-configuration>